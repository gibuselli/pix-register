spring:
  application:
    name: pix-register
  datasource:
    url: jdbc:postgresql://localhost:5432/PIXREGISTERDB
    username: local123
    password: local123
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
